/** @format */

//command - npm ls // to fetch npm modules ..

//npm version

// console.log(__dirname);
console.log(__filename);

let s = "NEWSLETTER";
var n = 3;
let p;
let q;
let r;
// console.log(s.length);
for (let i = 0; i <= s.length; i += 4) {
  //   console.log(s[i]);
  p = p + s[i];
}
for (let i = 1; i <= s.length; i += 2) {
  //   console.log(s[i]);
  q = q + s[i];
}
for (let i = 2; i <= s.length; i += 4) {
  //   console.log(s[i]);
  r = r + s[i];
}
console.log(p, q, r);

for (let i = 0; i <= s.length; i++) {}
//nle , wt
